apiVersion: apps/v1
kind: Deployment
metadata:
  name: simple-fastapi-app
spec:
  replicas: 2
  selector:
    matchLabels:
      app: simple-fastapi-app
  template:
    metadata:
      labels:
        app: simple-fastapi-app
    spec:
      containers:
      - name: fastapi
        image: saikishore29/simple-fastapi-app:af8997c1
        ports:
        - containerPort: 8000